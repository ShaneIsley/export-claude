javascript:(function() { 'use strict'; var CONFIG = { VERSION: '2.1.0', API_BASE: '/api', PAGE_SIZE: 50, CHAT_DELAY_MS: 120, PROJECT_DELAY_MS: 200, JSZIP_CDN: 'https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js' }; var Utils = { getOrgId: function() { var c = document.cookie.match(/lastActiveOrg=([^;]+)/); if (c) return c[1]; var entries = performance.getEntriesByType('resource'); for (var i = 0; i < entries.length; i++) { var m = entries[i].name.match(/\/api\/organizations\/([a-f0-9-]+)/); if (m) return m[1]; } return null; }, getProjectId: function() { var m = window.location.pathname.match(/\/project\/([a-f0-9-]+)/); return m ? m[1] : null; }, timestamp: function() { return new Date().toISOString().slice(0, 19).replace(/[:.]/g, '-'); }, sleep: function(ms) { return new Promise(function(r) { setTimeout(r, ms); }); }, safeName: function(n) { return (n || 'untitled').replace(/[^a-zA-Z0-9_\- ]/g, '').replace(/\s+/g, '-').replace(/-{2,}/g, '-').replace(/^-|-$/g, '').slice(0, 60) || 'untitled'; } }; function ApiClient(orgId) { this.base = CONFIG.API_BASE + '/organizations/' + orgId; } ApiClient.prototype.get = function(path) { var url = this.base + path; return fetch(url, { credentials: 'include', headers: { 'Content-Type': 'application/json' } }).then(function(r) { if (!r.ok) throw new Error('API ' + r.status + ': ' + path); return r.json(); }); }; function paginatedFetch(api, basePath, pageSize) { var all = []; var offset = 0; function fetchPage() { var sep = basePath.indexOf('?') >= 0 ? '&' : '?'; var path = basePath + sep + 'limit=' + pageSize + '&offset=' + offset; return api.get(path).then(function(resp) { var items, hasMore; if (resp && Array.isArray(resp.data)) { items = resp.data; hasMore = resp.pagination && resp.pagination.has_more; } else if (Array.isArray(resp)) { items = resp; hasMore = items.length >= pageSize; } else { items = []; hasMore = false; } all = all.concat(items); if (hasMore) { offset += pageSize; return Utils.sleep(80).then(fetchPage); } return all; }); } return fetchPage(); } function extractProjectList(api) { return paginatedFetch(api, '/projects_v2?is_archived=false', CONFIG.PAGE_SIZE); } function extractProjectMeta(api, projectId) { return api.get('/projects/' + projectId).then(function(d) { return { id: d.uuid || projectId, name: d.name || '', description: d.description || '', instructions: d.prompt_template || d.custom_instructions || '', model: d.model || '', created_at: d.created_at || null, updated_at: d.updated_at || null, is_starred: !!d.is_starred }; }); } function extractProjectChatList(api, projectId) { return paginatedFetch(api, '/projects/' + projectId + '/conversations_v2', CONFIG.PAGE_SIZE); } function extractOrphanChatList(api) { return paginatedFetch(api, '/chat_conversations', CONFIG.PAGE_SIZE); } function extractChatFull(api, chatId) { return api.get('/chat_conversations/' + chatId); } function extractProjectFiles(api, projectId) { var docsPromise = api.get('/projects/' + projectId + '/docs').then(function(docs) { return (docs || []).map(function(d) { return { id: d.uuid, filename: d.file_name || 'unknown', content: d.content || '', content_type: d.content_type || '', created_at: d.created_at, file_size: d.file_size || 0, file_kind: 'doc' }; }); }).catch(function() { return []; }); var blobsPromise = api.get('/projects/' + projectId + '/files').then(function(files) { return (files || []).map(function(f) { return { id: f.uuid || f.file_uuid, filename: f.file_name || 'unknown', content: '', content_type: '', created_at: f.created_at, file_size: f.size_bytes || 0, file_kind: f.file_kind || 'blob', path: f.path || '' }; }); }).catch(function() { return []; }); return Promise.all([docsPromise, blobsPromise]).then(function(results) { var docs = results[0]; var blobs = results[1]; var docIds = {}; for (var i = 0; i < docs.length; i++) { docIds[docs[i].id] = true; } var uniqueBlobs = blobs.filter(function(b) { return !docIds[b.id]; }); return docs.concat(uniqueBlobs); }); } function mapChatMessages(f) { return { id: f.uuid, name: f.name || '', project_uuid: f.project_uuid || null, created_at: f.created_at, updated_at: f.updated_at, model: f.model || '', messages: (f.chat_messages || []).map(function(m) { return { role: m.sender, text: m.text || '', content: m.content || null, created_at: m.created_at, attachments: (m.attachments || []).map(function(a) { return { id: a.id, file_name: a.file_name, file_type: a.file_type, file_size: a.file_size }; }) }; }) }; } function fetchFullChats(api, chatList, onProgress) { var results = []; var i = 0; function next() { if (i >= chatList.length) return Promise.resolve(results); var c = chatList[i]; var label = c.name || 'Chat ' + (i + 1); if (onProgress) onProgress('Chat ' + (i + 1) + '/' + chatList.length + ': ' + label); return extractChatFull(api, c.uuid).then(function(f) { results.push(mapChatMessages(f)); }).catch(function(e) { results.push({ id: c.uuid, name: label, project_uuid: c.project_uuid || null, error: e.message, messages: [] }); }).then(function() { i++; if (i < chatList.length) return Utils.sleep(CONFIG.CHAT_DELAY_MS).then(next); return results; }); } return next(); } function assembleCurrentProject(api, projectId, onProgress) { var meta, chats, files; onProgress('Fetching project metadata...'); return extractProjectMeta(api, projectId).then(function(m) { meta = m; onProgress('Listing project chats...'); return extractProjectChatList(api, projectId); }).then(function(chatList) { onProgress('Found ' + chatList.length + ' chats in this project'); return fetchFullChats(api, chatList, onProgress); }).then(function(c) { chats = c; onProgress('Fetching project files...'); return extractProjectFiles(api, projectId); }).then(function(f) { files = f; var proj = Object.assign({}, meta, { chats: chats, files: files }); return wrapExport([proj], []); }); } function assembleAll(api, onProgress) { var projectsMeta = []; var allProjectChats = {}; var orphanChats = []; var rawProjects; onProgress('Listing all projects...'); return extractProjectList(api).then(function(rp) { rawProjects = rp; onProgress('Found ' + rawProjects.length + ' projects'); var i = 0; function nextProject() { if (i >= rawProjects.length) return Promise.resolve(); var rp = rawProjects[i]; var pid = rp.uuid; onProgress('Project ' + (i + 1) + '/' + rawProjects.length + ': ' + (rp.name || 'Untitled')); return extractProjectMeta(api, pid).then(function(meta) { return extractProjectChatList(api, pid).then(function(chatList) { onProgress(' ' + chatList.length + ' chats'); return fetchFullChats(api, chatList, onProgress).then(function(chats) { return extractProjectFiles(api, pid).then(function(files) { projectsMeta.push(Object.assign({}, meta, { chats: chats, files: files })); }); }); }); }).catch(function(e) { projectsMeta.push({ id: pid, name: rp.name || 'Untitled', description: '', instructions: '', model: '', created_at: rp.created_at, updated_at: rp.updated_at, error: e.message, chats: [], files: [] }); }).then(function() { i++; if (i < rawProjects.length) return Utils.sleep(CONFIG.PROJECT_DELAY_MS).then(nextProject); }); } return nextProject(); }).then(function() { onProgress('Fetching non-project chats...'); return extractOrphanChatList(api); }).then(function(orphanList) { var projectChatIds = {}; for (var p = 0; p < projectsMeta.length; p++) { var pchats = projectsMeta[p].chats || []; for (var c = 0; c < pchats.length; c++) { projectChatIds[pchats[c].id] = true; } } var trueOrphans = orphanList.filter(function(c) { return !projectChatIds[c.uuid]; }); onProgress('Found ' + trueOrphans.length + ' non-project chats'); if (trueOrphans.length === 0) return []; return fetchFullChats(api, trueOrphans, onProgress); }).then(function(oc) { orphanChats = oc || []; return wrapExport(projectsMeta, orphanChats); }); } function wrapExport(projects, orphanChats) { var totalChats = orphanChats.length; var totalMessages = orphanChats.reduce(function(s, c) { return s + (c.messages || []).length; }, 0); var totalFiles = 0; var erroredChats = orphanChats.filter(function(c) { return c.error; }).length; for (var i = 0; i < projects.length; i++) { var p = projects[i]; totalChats += (p.chats || []).length; totalMessages += (p.chats || []).reduce(function(s, c) { return s + (c.messages || []).length; }, 0); totalFiles += (p.files || []).filter(function(f) { return !f.error; }).length; erroredChats += (p.chats || []).filter(function(c) { return c.error; }).length; } return { _format: 'claude-project-export', _version: CONFIG.VERSION, exported_at: new Date().toISOString(), source_url: window.location.href, projects: projects, orphan_chats: orphanChats, stats: { total_projects: projects.length, total_chats: totalChats, total_messages: totalMessages, total_files: totalFiles, errored_chats: erroredChats, orphan_chats: orphanChats.length } }; } var Markdown = { formatChat: function(chat) { var lines = ['---', 'title: ' + JSON.stringify(chat.name || 'Untitled'), 'id: ' + chat.id]; if (chat.project_uuid) lines.push('project: ' + chat.project_uuid); if (chat.model) lines.push('model: ' + chat.model); if (chat.created_at) lines.push('created: ' + chat.created_at); if (chat.updated_at) lines.push('updated: ' + chat.updated_at); lines.push('messages: ' + (chat.messages || []).length, '---', '', '# ' + (chat.name || 'Untitled Chat'), ''); if (chat.error) return lines.concat(['> **Export Error:** ' + chat.error, '']).join('\n'); var msgs = chat.messages || []; for (var i = 0; i < msgs.length; i++) { var msg = msgs[i]; var label = msg.role === 'human' ? 'Human' : msg.role === 'assistant' ? 'Assistant' : msg.role; lines.push('## ' + label, ''); if (msg.text) { lines.push(msg.text); } else if (Array.isArray(msg.content)) { for (var j = 0; j < msg.content.length; j++) { var b = msg.content[j]; if (b && b.type === 'text') lines.push(b.text || ''); } } lines.push(''); if (msg.attachments && msg.attachments.length) { for (var k = 0; k < msg.attachments.length; k++) lines.push('\u{1F4CE} `' + msg.attachments[k].file_name + '`'); lines.push(''); } lines.push('---', ''); } return lines.join('\n'); }, formatProjectReadme: function(project, exportData) { var lines = ['# ' + (project.name || 'Untitled Project'), '']; if (project.description) lines.push(project.description, ''); lines.push('| Field | Value |', '|-------|-------|'); lines.push('| **ID** | `' + project.id + '` |'); if (project.model) lines.push('| **Model** | `' + project.model + '` |'); if (project.created_at) lines.push('| **Created** | ' + project.created_at + ' |'); if (project.updated_at) lines.push('| **Updated** | ' + project.updated_at + ' |'); if (exportData) lines.push('| **Exported** | ' + exportData.exported_at + ' |'); lines.push(''); if (project.instructions) lines.push('## Custom Instructions', '', '```', project.instructions, '```', ''); var chats = project.chats || []; if (chats.length) { lines.push('## Chats (' + chats.length + ')', ''); for (var i = 0; i < chats.length; i++) { var c = chats[i]; var fname = String(i + 1).padStart(3, '0') + '-' + Utils.safeName(c.name || 'Untitled') + '.md'; var n = (c.messages || []).length; var d = (c.created_at || '').slice(0, 10); lines.push('- [' + (c.name || 'Untitled') + '](chats/' + fname + ') \u2014 ' + n + ' messages' + (d ? ' \u00B7 ' + d : '')); } lines.push(''); } var files = (project.files || []).filter(function(f) { return !f.error; }); var docFiles = files.filter(function(f) { return f.file_kind !== 'blob'; }); var blobFiles = files.filter(function(f) { return f.file_kind === 'blob'; }); if (docFiles.length) { lines.push('## Knowledge Files (' + docFiles.length + ')', ''); for (var j = 0; j < docFiles.length; j++) lines.push('- [' + docFiles[j].filename + '](files/' + docFiles[j].filename + ')'); lines.push(''); } if (blobFiles.length) { lines.push('## Uploaded Files \u2014 metadata only (' + blobFiles.length + ')', ''); for (var bj = 0; bj < blobFiles.length; bj++) { var bf = blobFiles[bj]; var sz = bf.file_size ? ' (' + bf.file_size + ' bytes)' : ''; lines.push('- `' + bf.filename + '`' + sz); } lines.push(''); } return lines.join('\n'); }, formatRootReadme: function(data) { var s = data.stats; var lines = ['# Claude Export', '', 'Exported: ' + data.exported_at, '', '## Stats', '', '- **Projects:** ' + s.total_projects, '- **Total chats:** ' + s.total_chats, '- **Total messages:** ' + s.total_messages, '- **Knowledge files:** ' + s.total_files]; if (s.orphan_chats) lines.push('- **Non-project chats:** ' + s.orphan_chats); if (s.errored_chats) lines.push('- **Errored chats:** ' + s.errored_chats); lines.push(''); if (data.projects.length) { lines.push('## Projects', ''); for (var i = 0; i < data.projects.length; i++) { var p = data.projects[i]; lines.push('- [' + (p.name || 'Untitled') + '](' + Utils.safeName(p.name) + '/) \u2014 ' + (p.chats || []).length + ' chats'); } lines.push(''); } if (data.orphan_chats.length) { lines.push('## Non-Project Chats (' + data.orphan_chats.length + ')', ''); for (var j = 0; j < data.orphan_chats.length; j++) { var c = data.orphan_chats[j]; var fname = String(j + 1).padStart(3, '0') + '-' + Utils.safeName(c.name || 'Untitled') + '.md'; lines.push('- [' + (c.name || 'Untitled') + '](_no-project/chats/' + fname + ')'); } lines.push(''); } return lines.join('\n'); } }; var ArtifactParser = { extract: function(msg) { var results = []; var text = msg.text || ''; var re = /<antArtifact([^>]*)>([\s\S]*?)<\/antArtifact>/g; var attrRe = /(\w+)="([^"]*)"/g; var m; while ((m = re.exec(text)) !== null) { var attrs = {}, am; attrRe.lastIndex = 0; while ((am = attrRe.exec(m[1])) !== null) attrs[am[1]] = am[2]; results.push({ filename: attrs.title || attrs.identifier || 'artifact', content: m[2].trim(), language: attrs.language || attrs.type || '' }); } if (Array.isArray(msg.content)) { for (var i = 0; i < msg.content.length; i++) { var b = msg.content[i]; if (b && b.type === 'tool_use' && (b.name || '').toLowerCase().indexOf('artifact') >= 0) { var inp = b.input || {}; results.push({ filename: inp.title || inp.id || 'artifact', content: inp.content || '', language: inp.language || inp.type || '' }); } } } return results; }, guessExt: function(lang, name) { if (name.indexOf('.') >= 0) return ''; var map = { python:'.py',javascript:'.js',typescript:'.ts',tsx:'.tsx',jsx:'.jsx',html:'.html',css:'.css',json:'.json',yaml:'.yaml',markdown:'.md',bash:'.sh',shell:'.sh',sql:'.sql',rust:'.rs',go:'.go',java:'.java',react:'.jsx',vue:'.vue',ruby:'.rb',php:'.php',swift:'.swift',cpp:'.cpp',c:'.c',toml:'.toml',xml:'.xml' }; return map[(lang || '').toLowerCase()] || '.txt'; } }; function buildZip(data) { var zip = new JSZip(); if (data.projects.length > 1 || data.orphan_chats.length) { zip.file('README.md', Markdown.formatRootReadme(data)); } var usedDirs = {}; for (var pi = 0; pi < data.projects.length; pi++) { var proj = data.projects[pi]; var dir = Utils.safeName(proj.name || 'Untitled'); if (usedDirs[dir]) dir += '-' + (proj.id || '').slice(0, 8); usedDirs[dir] = true; var folder = zip.folder(dir); folder.file('README.md', Markdown.formatProjectReadme(proj, data)); var chats = proj.chats || []; for (var ci = 0; ci < chats.length; ci++) { var chat = chats[ci]; var cfn = String(ci + 1).padStart(3, '0') + '-' + Utils.safeName(chat.name || 'Untitled') + '.md'; folder.file('chats/' + cfn, Markdown.formatChat(chat)); var msgs = chat.messages || []; for (var mi = 0; mi < msgs.length; mi++) { if (msgs[mi].role !== 'assistant') continue; var arts = ArtifactParser.extract(msgs[mi]); for (var ai = 0; ai < arts.length; ai++) { var sn = Utils.safeName(arts[ai].filename); var ext = ArtifactParser.guessExt(arts[ai].language, arts[ai].filename); if (ext && sn.slice(-ext.length) !== ext) sn += ext; folder.file('files/artifacts/' + sn, arts[ai].content); } } } var files = proj.files || []; for (var fi = 0; fi < files.length; fi++) { if (files[fi].error) continue; if (!files[fi].content) continue; folder.file('files/' + (files[fi].filename || 'unknown'), files[fi].content || ''); } } if (data.orphan_chats.length) { var of = zip.folder('_no-project'); for (var oi = 0; oi < data.orphan_chats.length; oi++) { var oc = data.orphan_chats[oi]; var ofn = String(oi + 1).padStart(3, '0') + '-' + Utils.safeName(oc.name || 'Untitled') + '.md'; of.file('chats/' + ofn, Markdown.formatChat(oc)); } } return zip.generateAsync({ type: 'blob', compression: 'DEFLATE', compressionOptions: { level: 6 } }); } function downloadBlob(blob, filename) { var url = URL.createObjectURL(blob); var a = document.createElement('a'); a.href = url; a.download = filename; document.body.appendChild(a); a.click(); a.remove(); setTimeout(function() { URL.revokeObjectURL(url); }, 5000); } function exportFilename(data, ext) { var prefix = 'claude-export'; var name = data.projects.length === 1 ? prefix + '-' + Utils.safeName(data.projects[0].name) : prefix + '-all'; return name + '-' + Utils.timestamp() + ext; } function downloadJson(data) { var blob = new Blob([JSON.stringify(data, null, 2)], { type: 'application/json' }); downloadBlob(blob, exportFilename(data, '.json')); } function downloadZip(data) { return buildZip(data).then(function(blob) { downloadBlob(blob, exportFilename(data, '.zip')); }); } function loadScript(url) { return new Promise(function(resolve, reject) { if (typeof JSZip !== 'undefined') return resolve(); var s = document.createElement('script'); s.src = url; s.onload = resolve; s.onerror = function() { reject(new Error('Failed to load JSZip from CDN')); }; document.head.appendChild(s); }); } function showDialog(currentProjectId) { return new Promise(function(resolve) { var existing = document.getElementById('__cpe_dialog'); if (existing) existing.remove(); var el = document.createElement('div'); el.id = '__cpe_dialog'; var onProj = !!currentProjectId; function opt(group, value, label, desc, checked, disabled) { return '<label style="display:flex;align-items:center;gap:10px;padding:10px 14px;background:#0f172a;border-radius:10px;border:1px solid #1e293b;cursor:' + (disabled ? 'not-allowed' : 'pointer') + ';margin-bottom:6px;opacity:' + (disabled ? '.5' : '1') + '">' + '<input type="radio" name="' + group + '" value="' + value + '"' + (checked ? ' checked' : '') + (disabled ? ' disabled' : '') + ' style="accent-color:#818cf8">' + '<div><div style="font-size:13px;color:#e2e8f0">' + label + '</div>' + '<div style="font-size:11px;color:#64748b">' + desc + '</div></div></label>'; } el.innerHTML = '<div style="position:fixed;inset:0;background:rgba(0,0,0,.6);z-index:99999;display:flex;align-items:center;justify-content:center;font-family:-apple-system,BlinkMacSystemFont,sans-serif">' + '<div style="background:#1a1a2e;color:#e0e0e0;border-radius:16px;padding:32px 36px;min-width:400px;max-width:480px;box-shadow:0 24px 48px rgba(0,0,0,.5)">' + '<div style="font-size:15px;font-weight:700;color:#818cf8;margin-bottom:4px">\u{1F4E6} Claude Project Exporter</div>' + '<div style="font-size:12px;color:#64748b;margin-bottom:20px">v' + CONFIG.VERSION + '</div>' + '<div style="margin-bottom:18px">' + '<div style="font-size:12px;font-weight:600;color:#94a3b8;text-transform:uppercase;letter-spacing:.5px;margin-bottom:8px">Scope</div>' + opt('__cpe_scope', 'current', 'Current project only', onProj ? 'Export the project you\u2019re viewing' : 'Navigate to a project page first', onProj, !onProj) + opt('__cpe_scope', 'all', 'All projects + non-project chats', 'Every project with its files, plus standalone chats', !onProj, false) + '</div>' + '<div style="margin-bottom:22px">' + '<div style="font-size:12px;font-weight:600;color:#94a3b8;text-transform:uppercase;letter-spacing:.5px;margin-bottom:8px">Format</div>' + opt('__cpe_fmt', 'json', 'JSON', 'Raw structured data \u2014 for scripting & reimport', true, false) + opt('__cpe_fmt', 'zip', 'ZIP of project files', 'Markdown chats, knowledge files, extracted artifacts', false, false) + '</div>' + '<div style="display:flex;gap:10px;justify-content:flex-end">' + '<button id="__cpe_cancel" style="padding:8px 20px;background:#1e293b;color:#94a3b8;border:1px solid #334155;border-radius:8px;cursor:pointer;font-size:13px">Cancel</button>' + '<button id="__cpe_go" style="padding:8px 24px;background:linear-gradient(135deg,#6366f1,#818cf8);color:#fff;border:none;border-radius:8px;cursor:pointer;font-size:13px;font-weight:600">Export</button>' + '</div></div></div>'; document.body.appendChild(el); function close(result) { el.remove(); resolve(result); } el.querySelector('#__cpe_cancel').onclick = function() { close(null); }; el.firstChild.addEventListener('click', function(e) { if (e.target === el.firstChild) close(null); }); el.querySelector('#__cpe_go').onclick = function() { var scope = (el.querySelector('input[name="__cpe_scope"]:checked') || {}).value; var format = (el.querySelector('input[name="__cpe_fmt"]:checked') || {}).value; close({ scope: scope, format: format }); }; }); } function ProgressUI() { this.el = document.createElement('div'); this.el.id = '__cpe_progress'; this.el.innerHTML = '<div style="position:fixed;inset:0;background:rgba(0,0,0,.55);z-index:99999;display:flex;align-items:center;justify-content:center;font-family:-apple-system,BlinkMacSystemFont,sans-serif">' + '<div style="background:#1a1a2e;color:#e0e0e0;border-radius:16px;padding:32px 40px;min-width:400px;max-width:520px;box-shadow:0 24px 48px rgba(0,0,0,.4);text-align:center">' + '<div style="font-size:14px;font-weight:600;color:#818cf8;margin-bottom:6px">EXPORTING</div>' + '<div id="__cpe_status" style="font-size:14px;margin:16px 0;min-height:24px;color:#d1d5db">Initializing...</div>' + '<div style="background:#2d2d44;border-radius:8px;height:6px;overflow:hidden;margin:16px 0">' + '<div id="__cpe_bar" style="background:linear-gradient(90deg,#818cf8,#a78bfa);height:100%;width:5%;transition:width .3s;border-radius:8px"></div></div>' + '<div id="__cpe_stats" style="font-size:12px;color:#9ca3af;margin-top:8px"></div>' + '<button id="__cpe_close" style="display:none;margin-top:16px;padding:8px 24px;background:#818cf8;color:#fff;border:none;border-radius:8px;cursor:pointer;font-size:13px">Close</button>' + '</div></div>'; } ProgressUI.prototype.show = function() { var old = document.getElementById('__cpe_progress'); if (old) old.remove(); document.body.appendChild(this.el); var self = this; document.getElementById('__cpe_close').onclick = function() { self.hide(); }; }; ProgressUI.prototype.update = function(msg, pct) { var s = document.getElementById('__cpe_status'); var b = document.getElementById('__cpe_bar'); if (s) s.textContent = msg; if (b && pct != null) b.style.width = Math.min(100, pct) + '%'; }; ProgressUI.prototype.done = function(msg, stats) { this.update(msg, 100); var t = document.getElementById('__cpe_stats'); if (t && stats) t.textContent = stats.total_projects + ' projects \u00B7 ' + stats.total_chats + ' chats \u00B7 ' + stats.total_messages + ' msgs \u00B7 ' + stats.total_files + ' files'; document.getElementById('__cpe_close').style.display = 'inline-block'; }; ProgressUI.prototype.error = function(msg) { this.update('Error: ' + msg, 0); var b = document.getElementById('__cpe_bar'); if (b) b.style.background = '#ef4444'; document.getElementById('__cpe_close').style.display = 'inline-block'; }; ProgressUI.prototype.hide = function() { this.el.remove(); }; var projectId = Utils.getProjectId(); showDialog(projectId).then(function(choice) { if (!choice) return; var ui = new ProgressUI(); ui.show(); var orgId = Utils.getOrgId(); if (!orgId) { ui.error('Could not detect org ID. Try refreshing.'); return; } var p = Promise.resolve(); if (choice.format === 'zip') { ui.update('Loading ZIP library...', 5); p = loadScript(CONFIG.JSZIP_CDN); } p.then(function() { ui.update('Connecting...', 10); var api = new ApiClient(orgId); var step = 0; var onProgress = function(msg) { step++; ui.update(msg, Math.min(10 + step * 3, 90)); }; if (choice.scope === 'current') { if (!projectId) throw new Error('Not on a project page. Use "All projects" or navigate to one.'); return assembleCurrentProject(api, projectId, onProgress); } else { return assembleAll(api, onProgress); } }).then(function(data) { ui.update('Preparing download...', 95); if (choice.format === 'zip') { return downloadZip(data).then(function() { return data; }); } else { downloadJson(data); return data; } }).then(function(data) { ui.done('Export complete!', data.stats); }).catch(function(e) { console.error('[Claude Export]', e); ui.error(e.message); }); }); })();
